import {
    
} from "../_action-types"

const initialState = {
    
}

export default function contractReducer(state = initialState, action) {
    switch (action.type) {
        default:
            break
    }
    return state
}
